# Build output
/dist
/target
/src-tauri/gen/schemas

# Dependencies
/node_modules

# Local runtime data — machine-specific, not source
tetra.db
tetra.db-shm
tetra.db-wal

# Project notes kept local, not published
# Design-rationale notes mirroring the source tree
.ai-notes/

# Editor / tooling
.claude/
*.log

# Agent instructions — local only, not published
CLAUDE.md
AGENTS.md

# UI overhaul screenshots — not committed
screenshots/

# Backend deployment secrets — holds the Steam Web API key
deploy/.env
