# Build output
/dist
/target
/src-tauri/gen/schemas

# Dependencies
/node_modules

# Local runtime data — machine-specific, not source
tetra.db
tetra.db-shm
tetra.db-wal

# Project notes kept local, not published
progress.md
implementation_plan.md
.ai-notes/

# Editor / tooling
.claude/
*.log

# Agent instructions — local only, not published
CLAUDE.md
AGENTS.md

# UI overhaul mockups & screenshots — not committed
mockups/
screenshots/

# impeccable — local only, not published
.impeccable/
