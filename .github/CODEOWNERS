# Paths that change what AI agents DO in this repository, not merely what they
# know. Same review posture as CI configuration, for the same reason: a workflow
# file is executable content.
#
# Honest caveat: on a solo repository GitHub cannot require you to review your
# own pull request, so today this is documentation. It starts having teeth the
# moment someone else contributes — which is exactly the situation it exists
# for.

/AGENTS.md                  @HellboundGlory

# Existing CI, same reasoning — release.yml is what publishes.
/.github/workflows/         @HellboundGlory
